services:
  app:
    image: freshrss/freshrss:latest
    restart: unless-stopped
    ports:
      - "${SELFSTACK_HOST_PORT:-80}:80"
    volumes:
      - freshrss-data:/var/www/FreshRSS/data
      - freshrss-extensions:/var/www/FreshRSS/extensions

volumes:
  freshrss-data:
  freshrss-extensions:
