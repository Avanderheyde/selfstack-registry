services:
  app:
    image: deluan/navidrome:latest
    restart: unless-stopped
    ports:
      - "${SELFSTACK_HOST_PORT:-4533}:4533"
    volumes:
      - navidrome-data:/data
      - navidrome-music:/music

volumes:
  navidrome-data:
  navidrome-music:
