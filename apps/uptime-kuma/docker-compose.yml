services:
  app:
    image: louislam/uptime-kuma:1
    restart: unless-stopped
    ports:
      - "${SELFSTACK_HOST_PORT:-3001}:3001"
    volumes:
      - uptime-kuma-data:/app/data

volumes:
  uptime-kuma-data:
